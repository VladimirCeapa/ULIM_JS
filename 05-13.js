/*
Write a JavaScript program to add items in an blank array and display the items.
*/
let newArr = [];
let addToArr = (num, arr) => {
    return arr.push(num);
}
addToArr(5, newArr);
addToArr(9, newArr);
